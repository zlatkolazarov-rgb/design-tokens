# Sync hook test — 2026-05-26

Test file written to verify the auto-sync hook picks up untracked files
after fixing the `git diff` early-out (porcelain check now used instead).

Safe to delete after the sync log shows `synced: …`.
